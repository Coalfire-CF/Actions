name: Terraform Validate

on:
  workflow_call:
    inputs:
      terraform_version:
        description: 'The version of Terraform to use (leave empty to use .terraform-version)'
        required: false
        default: ''
        type: string
      slack_channel_id:
        description: 'Slack channel ID for failure notifications (optional)'
        required: false
        type: string
        default: ''
    secrets:
      TF_VALIDATE_APP_CLIENT_ID:
        description: 'The GitHub App client ID for Terraform authentication (only needed for private repositories)'
        required: false
      TF_VALIDATE_APP_PRIVATE_KEY:
        description: 'The GitHub App private key for Terraform authentication (only needed for private repositories)'
        required: false

  workflow_dispatch:
    inputs:
      terraform_version:
        description: 'The version of Terraform to use (leave empty to use .terraform-version)'
        required: false
        default: ''
        type: string

jobs:
  verify:
    name: Validate Terraform Configuration
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v6

    - name: Resolve Terraform version
      id: tf-version
      run: |
        if [ -n "${{ inputs.terraform_version }}" ]; then
          echo "version=${{ inputs.terraform_version }}" >> $GITHUB_OUTPUT
        elif [ -f .terraform-version ]; then
          echo "version=$(cat .terraform-version | tr -d '[:space:]')" >> $GITHUB_OUTPUT
        else
          VERSION=$(curl -s https://raw.githubusercontent.com/Coalfire-CF/Actions/main/.terraform-version | tr -d '[:space:]')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
        fi

    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v4
      with:
        terraform_version: ${{ steps.tf-version.outputs.version }}

    - name: Check for secrets
      id: check-secrets
      run: |
        if [ -n "$TF_VALIDATE_APP_CLIENT_ID" ] && [ -n "$TF_VALIDATE_APP_PRIVATE_KEY" ]; then
          echo "has_secrets=true" >> $GITHUB_OUTPUT
        else
          echo "has_secrets=false" >> $GITHUB_OUTPUT
        fi
      env:
        TF_VALIDATE_APP_CLIENT_ID: ${{ secrets.TF_VALIDATE_APP_CLIENT_ID }}
        TF_VALIDATE_APP_PRIVATE_KEY: ${{ secrets.TF_VALIDATE_APP_PRIVATE_KEY }}
        
    - name: Get Checkout Token
      id: checkout-token
      if: steps.check-secrets.outputs.has_secrets == 'true'
      uses: actions/create-github-app-token@v3
      with:
        app-id: ${{ secrets.TF_VALIDATE_APP_CLIENT_ID }}
        private-key: ${{ secrets.TF_VALIDATE_APP_PRIVATE_KEY }}
        owner: ${{ github.repository_owner }}
    
    - name: Configure Git
      if: steps.check-secrets.outputs.has_secrets == 'true'
      run: |
        git config --global url."https://actions:${{ steps.checkout-token.outputs.token }}@github.com".insteadOf https://github.com
        git config --global url."https://actions:${{ steps.checkout-token.outputs.token }}@github.com/".insteadOf ssh://git@github.com/ 
        
    - name: Initialise with no backend
      run: terraform init -backend=false

    - name: Validate Terraform
      id: validate
      run: |
        set +e
        OUTPUT=$(terraform validate)
        CLEAN_OUTPUT=$(echo "$OUTPUT" | sed -r "s/\x1B\[([0-9]{1,2}(;[0-9]{1,2})?)?[m|K]//g")
        echo "$CLEAN_OUTPUT"
        echo "result=$CLEAN_OUTPUT" >> $GITHUB_OUTPUT
        set -e
      continue-on-error: true

    - name: Create comment
      if: github.event_name == 'pull_request'
      uses: actions/github-script@v8
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        script: |
          const issue_number = context.issue.number;
          const result = `${{ steps.validate.outputs.result }}`;
          github.rest.issues.createComment({
            owner: context.repo.owner,
            repo: context.repo.repo,
            issue_number: issue_number,
            body: `Terraform validation output:\n\`\`\`\n${result}\n\`\`\``
          });
  notify-failure:
    needs: verify
    if: failure() && inputs.slack_channel_id != ''
    uses: Coalfire-CF/Actions/.github/workflows/org-slack-notify.yml@main
    secrets: inherit
    with:
      notification-type: failure
      channel-id: ${{ inputs.slack_channel_id }}
