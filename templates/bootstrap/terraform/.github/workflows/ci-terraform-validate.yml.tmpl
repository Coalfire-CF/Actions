name: "CI: Terraform validate"

permissions:
  contents: write
  pull-requests: write

on:
  pull_request:
    paths:
      - "**.tf"
      - "**.tfvars"
      - "**.hcl"
      - ".terraform.lock.hcl"
      - "**/terraform/**"

jobs:
  terraform-validate:
    name: Validate Terraform
    uses: Coalfire-CF/Actions/.github/workflows/ci-terraform-validate.yml@__ACTIONS_SHA__ # __ACTIONS_VERSION__
    with:
      terraform_version: "1.13.3"
    secrets: inherit
