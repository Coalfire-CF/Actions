name: "CI: Markdown"

permissions:
  contents: write
  pull-requests: write

on:
  pull_request:
    paths:
      - "**.md"
    branches:
      - "**"
  workflow_call:

jobs:
  check-markdown:
    name: Check Markdown
    uses: Coalfire-CF/Actions/.github/workflows/ci-markdown.yml@__ACTIONS_SHA__ # __ACTIONS_VERSION__
