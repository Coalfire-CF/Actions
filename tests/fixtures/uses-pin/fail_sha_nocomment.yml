# Fixture: INVALID (FAIL) — bare 40-hex SHA WITHOUT an adjacent `# vX.Y.Z`
# release comment (unauditable). The gate MUST flag this line.
jobs:
  build:
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
